import { MakeImportantDirective } from './make-important.directive';

describe('MakeImportantDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeImportantDirective();
    expect(directive).toBeTruthy();
  });
});
